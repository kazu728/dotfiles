# コミット件名ポリシー

直近のコミット件名から、支配的な件名スタイルを推定する。

スタイルが明確な場合は、次をできるだけ揃える。

- 同じ言語を使う。
- `feat:` `fix:` `chore:` `docs:` `refactor:` などの prefix を踏襲する。
- トーン・大文字小文字・句読点・長さを揃える。

スタイルが混在している、または手がかりがない場合は、英語・命令形の Git コミット件名を1行で書く。
何が・なぜ変わったかを要約する。

出力はコミット件名1行だけにする。
引用符・マークダウン・箇条書き・コードフェンス・説明は付けない。
