# Global instructions

- 回答は日本語で行う。
- 設定・実装はツールのデフォルト挙動を優先する。デフォルトで要件を満たせるなら、独自の style / ラベル / フォーマット等の上書き設定を足さない。
- 既存コードへの継ぎ足し（論理的凝集）で済ませず、触れる範囲は1から設計したような構造で実装する。ただし変更範囲そのものは最小限に保つ。
- 変更は最小限・最小差分にとどめ、要求された範囲を超えて広げない。
- コードレビューは実装したコンテキストで結論を出さず、fresh subagent に diff だけ渡して実行させる（実装者バイアスの排除）。実装意図や弁明は渡さない。日常は /code-review high、徹底時は deep-review。
- 返ってきた指摘は握りつぶさず、提示で止めずに main セッションで直す。1つの変更に対する往復も main セッションが数え、4ラウンド（レビュー→修正で1）を上限とする。上限で指摘が残っていれば、それを添えて人間に判断を返す。
- レビュアーは、フォーマッタや静的解析が自動修正できる体裁・不使用コードを指摘しない。正しさとセキュリティの指摘は、機械検査の有無にかかわらず出す。
- コードから読み取れる自明なコメントは書かない。コメントを残すのは意図（Why / Why not）を補うときだけ。
